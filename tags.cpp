#include <iostream>
#include <vector>
#include <string>
using namespace std;

int main(){
    int n, q;
    cin >> n >> q;
    string line;
    getline(cin, line);
    cout << line << '\n';
    return 0;
}
